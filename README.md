## Discourse load more button

A simple Discourse theme component that adds a load more button to interrupt the infinite scroll behaviour. Also includes a setting to toggle it off for anonymous users (not logged in).

more info here: https://meta.discourse.org/t/load-more-button/402049/
